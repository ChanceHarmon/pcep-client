function printForInRange() {
  return (

    <div data-datacamp-exercise data-lang="python">
      <code data-type="pre-exercise-code">
        # no pec
      </code>
      <code data-type="sample-code">
        # Print out the numbers 1-10 using a `for in range` loop.<br />
      </code>
      <code data-type="solution">
        # Create a for in loop<br />
        for i in range(1,10):<br />
        # Print out i<br />
        print(i)
      </code>
      <code data-type="sct">
      </code>
      <div data-type="hint">
        Use the assignment operator (<code>=</code>) to create the variable <code>a</code>.
      </div>
    </div>
  )
}

export default printForInRange;